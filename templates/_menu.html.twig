<nav class="navbar navbar-expand-lg navbar-dark bg-dark custom-navbar">
	<div
		class="container-fluid">
		<!-- Logo -->
		<a class="navbar-brand fw-bold" href="#">BiblioSF6</a>

		<!-- Bouton toggle pour le menu mobile -->
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<!-- Menu de navigation -->
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul
				class="navbar-nav me-auto mb-2 mb-lg-0">
				<!-- Lien vers accueil -->
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
				</li>
				<!-- Favoris -->
				<li class="nav-item">
					<a class="nav-link" href="">Favoris</a>
				</li>
				{# catégories #}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_categories') }}">Catégorie</a>
				</li>


				<!-- Menu admin uniquement -->
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Gestion
						</a>
						<ul class="dropdown-menu custom-dropdown-menu">
							<li>
								<a class="dropdown-item" href="{{ path('app_livre') }}">Livre</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_nouveau') }}">Ajout de livre</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_livre_categories') }}">catégorie</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_abonne_index') }}">Abonné</a>
							</li>
							<li>
								<a class="dropdown-item" href="#">Emprunt</a>
							</li>
						</ul>
					</li>
				{% endif %}
			</ul>

			<!-- Formulaire de recherche -->
			<form class="d-flex me-3" action="{{ path('search_books') }}" method="GET" role="search">
				<input class="form-control me-2 custom-search-input" type="search" name="q" placeholder="Rechercher un livre..." aria-label="Search">
				<button class="btn btn-outline-light" type="submit">
					<i class="bi bi-search"></i>
				</button>
			</form>

			<!-- Menu Authentification -->
			<ul class="navbar-nav">
				{% if app.user %}
					<li class="nav-item">
						<span class="nav-link">Bonjour
							{{ app.user.pseudo }}</span>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
